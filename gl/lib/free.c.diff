--- gnulib/lib/free.c	2025-12-04 15:00:30.570204049 +0000
+++ lib/free.c	2025-12-04 15:04:27.056275098 +0000
@@ -41,11 +41,11 @@
   err[0] = errno;
   err[1] = errno;
   errno = 0;
-  free (p);
+  GC_free (p);
   errno = err[errno == 0];
 # else
   int saved_errno = errno;
-  free (p);
+  GC_free (p);
   errno = saved_errno;
 # endif
 }
